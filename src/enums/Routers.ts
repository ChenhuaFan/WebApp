export enum Routers {
  HOME = '',
  LOGIN = '/login',
  REGISTER = '/register',
  SUBLEASE_POST = '/sublease/post',
  SHARING_POST = '/sharing/post',
  WIKI = '/wiki',
  ME = '/me'
}

export enum PostType {
  SUBLEASE = 'sublease',
  SHARING = 'share'
}